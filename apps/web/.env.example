# VITE_API_URL — Required in production — Render API URL — https://api.example.com
# VITE_WS_URL — Optional — secure realtime WebSocket URL — wss://api.example.com/ws
# VITE_SENTRY_DSN — Optional — Sentry project settings — https://examplePublicKey@o0.ingest.sentry.io/0
VITE_API_URL=http://localhost:8080
VITE_WS_URL=ws://localhost:8080/ws
VITE_SENTRY_DSN=
